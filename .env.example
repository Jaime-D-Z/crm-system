PORT=3000
SESSION_SECRET=CHANGE_ME_STRONG_SECRET

# ── MySQL ──────────────────────────────────────────────────
# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=crm_system
# Connection string variant (optional)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/crm_system

# ── JWT ────────────────────────────────────────────────────
JWT_SECRET=CHANGE_ME_STRONG_JWT_SECRET
JWT_REFRESH_SECRET=CHANGE_ME_STRONG_REFRESH_SECRET

# ── App ────────────────────────────────────────────────────
APP_NAME=CRM System
APP_URL=http://localhost:3000

# ── Email (Nodemailer) ─────────────────────────────────────
# Gmail: enable App Passwords in Google Account settings
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=tu-email@gmail.com
MAIL_PASS=tu-app-password-de-16-caracteres
MAIL_FROM=CRM System <tu-email@gmail.com>
